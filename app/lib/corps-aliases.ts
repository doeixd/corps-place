// Re-exported from the shared read-model builders so the live services and the
// emitter use one definition (see sdk/src/readModel/builders/corpsAliases.ts).
export { RELATED_CORPS_CTES } from '@sdk/src/readModel/builders/corpsAliases.js';
